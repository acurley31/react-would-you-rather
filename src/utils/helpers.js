import { formatQuestion } from './_DATA';


// Re-exporting for niceness
export const formatQuestion = ({ optionOneText, optionTwoText, author }) => {
    return formatQuestion({optionOneText, optionTwoText, author})
}
